#include "dates.h"
#include <stdlib.h>
#include <stdio.h>

int main(void)
{
    // Date d;
    // initialiseDate(&d);
    // afficheDate(&d);

    // Date d;
    // d = creerDateParCopie();
    // afficheDate(&d);

    // Date *d;
    // d = newDate();
    // afficheDate(d);
    // free(d);

    // Date d;
    // d.jour = 31;
    // d.mois = JANVIER;
    // d.annee = 2003;
    // printf("%s", dateValide(d)? "True": "False");

    // Date d;
    // d.jour = 31;
    // d.mois = MARS;
    // d.annee = 2003;
    // printf("%d", jourDansAnnee(d));

    return EXIT_SUCCESS;
}